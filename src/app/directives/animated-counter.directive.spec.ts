import { AnimatedCounterDirective } from './animated-counter.directive';

describe('AnimatedCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedCounterDirective();
    expect(directive).toBeTruthy();
  });
});
